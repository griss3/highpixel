function cambiarContenido(categoria) {
    // Aquí puedes realizar acciones específicas según la categoría
    // Por ahora, simplemente cambiaremos el texto de ejemplo
    document.getElementById('contenido').innerHTML = `<p>Contenido de ${categoria}</p>`;
}